In order to run the installation run the following commands:
    chmod +x install.sh
    ./install.sh

** You can uninstall this vim upgrade at any point by simply running:
    chmod +x uninstall.sh
    ./uninstall.sh
